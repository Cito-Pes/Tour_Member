# 여행고객 CRM 등록 (항차별) 사용자 매뉴얼

## 1. 프로그램 개요

항차별 여행고객 Excel 파일을 조회하고 회원번호를 할당한 뒤 고객관리 DB에 행사정보를 등록하는 프로그램이다. 프로그램 버전은 1.0이며 Windows에서 사용한다.

## 2. 실행 전 준비

- 사내 DB 서버에 접속할 수 있는 네트워크 환경이 필요하다.
- PC에 프로그램에서 사용하는 Microsoft SQL Server ODBC 드라이버가 설치되어 있어야 한다.
- 조회할 `.xlsx` 또는 `.xls` 파일을 준비한다.
- 프로그램과 같은 위치에 `xlsx` 폴더를 만들고 파일을 보관하면 `① 파일검색` 시 해당 폴더가 먼저 열린다.
- 최초 실행 시 EXE 옆의 `DB\Config_DB.db`가 자동으로 생성될 수 있다. 이 파일을 임의로 수정하지 않는다.

## 3. 화면 구성

### 왼쪽: 여행항차

DB에 저장된 행사코드별 항차를 표시한다.

- 파란색 `●`: 연결된 회원번호 자료가 있음
- 빨간색 `X`: 기초자료만 있고 연결된 회원번호 자료가 없음
- 항차를 클릭하면 해당 고객자료가 오른쪽 표에 표시된다.

### 오른쪽: 조회 및 처리

- 첫째 줄: 파일경로, `① 파일검색`, `② 조회`
- 둘째 줄: 행사코드, 여행지역, 출발일, 도착일, 인원당 계정수, 처리 버튼
- 중앙 표: Excel 또는 DB에서 조회한 고객자료
- 진행률 표시줄: 현재 작업 진행상태
- 처리현황: 날짜와 시각을 포함한 작업 로그

## 4. 기본 작업 순서

### 4.1 파일 선택

1. `① 파일검색`을 누른다.
2. `.xlsx` 또는 `.xls` 파일을 선택한다.
3. 선택한 경로와 파일명이 상단에 표시되는지 확인한다.

### 4.2 Excel 조회

1. `② 조회`를 누른다.
2. 행사코드, 여행지역, 출발일, 도착일이 올바르게 추출됐는지 확인한다.
3. 행사코드가 잘못 추출됐으면 행사코드 입력란에서 직접 수정한다.
4. 오른쪽 고객 표의 이름, 회원코드, 금액, 메모 등을 확인한다.

조회만으로는 왼쪽 여행항차 목록이나 DB에 자료가 저장되지 않는다.

### 4.3 인원당 계정수 선택

`1계정`부터 `4계정` 중 해당 항차의 인원당 계정수를 선택한다. 빈 값인 상태에서는 검토를 진행할 수 없다.

### 4.4 검토

1. `③ 검토`를 누른다.
2. 안내창에서 기초자료 저장 여부를 확인한다.
3. `예`를 선택하면 Excel 기초자료가 DB에 저장되고 회원번호 검토가 시작된다.
4. 회원번호 1~4에 예상한 계정이 할당됐는지 확인한다.
5. 오류가 표시되면 회원코드 또는 보유 계정수를 확인한다.

검토 단계에서는 행사 테이블에 등록하지 않는다. 기초자료 저장과 화면의 회원번호 할당까지만 진행한다.

### 4.5 적용

1. 검토 결과와 회원번호를 최종 확인한다.
2. `④ 적용`을 누른다.
3. 적용 대상 테이블 안내를 읽고 `예`를 선택한다.
4. `적용 완료` 메시지가 표시되는지 확인한다.
5. 왼쪽 항차 목록의 표시가 파란색 `●`로 변경됐는지 확인한다.

적용 시 다음 작업을 하나의 트랜잭션으로 처리한다.

- 회원번호 할당내역 저장
- 여행상품 확인 또는 생성
- 행사 및 여행자정보 저장
- 행사비용 저장
- 회원상태와 상태이력 저장
- 상담메모 저장

중간에 오류가 발생하면 적용 단계에서 처리한 내용은 전체 취소된다. 오류창에 표시되는 행 번호, 회원번호, 실패 단계를 확인해 관리자에게 전달한다.

### 4.6 Excel 다운로드

1. 화면에 원하는 자료가 표시된 상태에서 `📊 엑셀 다운로드`를 누른다.
2. 저장 위치와 파일명을 선택한다.
3. 생성된 새 Excel 파일을 확인한다.

원본 Excel 파일은 변경되지 않는다.

## 5. 자동 변환 규칙

### TOUR LEADER

예약상태가 `TOUR LEADER`이면 회원코드에 `TOUR LEADER`를 표시하고 예약상태는 비운다. 회원명이 없는 경우도 허용한다. 일반회원 계정 조회와 할당은 하지 않는다.

### 노쇼

메모에 `노쇼`가 있으면 여행상태에 `노쇼`를 표시한다. 적용 시 행사유형과 회원상태도 `노쇼`로 처리한다.

### 개인정보와 날짜

- 주민번호 뒷자리는 저장하지 않는다.
- 날짜는 `YYYY-MM-DD`로 저장한다.

### 행사비용

- Excel의 항만세, 유류할증료, 환율추가금, 선내팁, 객실추가, 항공추가, 비자금액, 추가금액을 행사비용으로 저장한다.
- 비어 있는 숫자형 비용은 `0`으로 저장한다.
- 객실추가가 0보다 크면 객실업그레이드 유무를 `1`로 저장한다.
- 항공추가가 0보다 크면 항공업그레이드 유무를 `1`로 저장한다.

## 6. 오류 대응

### 인원당 계정수를 선택하라는 메시지

인원당 계정수 콤보박스에서 값을 선택한 후 다시 검토한다.

### 기초자료를 저장하라는 메시지

Excel을 다시 조회하고 `③ 검토`를 먼저 완료한다. 적용은 같은 실행 화면에서 검토를 마친 후 진행한다.

### 회원번호를 찾지 못함

- 회원코드가 정확한지 확인한다.
- 해당 회원코드에 사용 가능한 계정이 DB에 존재하는지 확인한다.
- 필요한 계정수보다 보유 계정수가 부족하지 않은지 확인한다.

### 적용 오류

오류 메시지의 다음 정보를 기록한다.

- 고객자료 행 번호
- 회원번호
- 실패 단계
- DB 오류내용

오류가 발생한 적용 작업은 롤백되지만 검토 단계에서 저장된 기초자료는 남아 있을 수 있다.

### DB 설정 다운로드 실패

- 인터넷 또는 사내 네트워크 연결을 확인한다.
- EXE가 위치한 폴더에 파일을 생성할 권한이 있는지 확인한다.
- 보안 프로그램이 Google Drive 다운로드를 차단하는지 확인한다.

## 7. 배포 시 주의사항

- 배포 파일은 `여행고객 CRM 등록 (항차별).exe` 한 개다.
- 최초 실행 후 EXE 옆에 생성된 `DB` 폴더는 삭제하지 않는 것이 좋다.
- 원본 Excel 보관용 `xlsx` 폴더는 필요에 따라 별도로 만든다.
- 새 버전 EXE로 교체할 때 기존 `DB` 폴더는 그대로 유지할 수 있다.
